#include <QtCore>


class FirmwareUpgraderWatcher
{
    SLOT(void start(QString firmwareFilename));

    SIGNAL(subsystemStateChanged(QString subsystem, uint state));
    SIGNAL(flasherProgressChanged(uint value));
};
